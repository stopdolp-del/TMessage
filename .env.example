# Server (PORT used when TMESSING_AUTO_PORT is not set, e.g. `node server/index.js`)
PORT=3000
JWT_SECRET=change-this-to-a-long-random-string-in-production
NODE_ENV=development

# Main admin account (username must match; promoted on startup if user exists)
ADMIN_USERNAME=stopdolp
